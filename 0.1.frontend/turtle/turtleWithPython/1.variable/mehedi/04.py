from turtle import Turtle, done

color = "green"
length = 100
angle = 90

t = Turtle()
t.color(color)

t.forward(length)
t.right(angle)
t.forward(length)
t.right(angle)
t.forward(length)
t.right(angle)
t.forward(length)

done()